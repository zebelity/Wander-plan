<h2 class="subheading">Edit Trip</h2>

<form class="form" action="/trips/<%= trip.id %>" method="post">
  <input type="hidden" name="_method" value="put">
  <label for="">Title</label>
  <input type="text" name="title" value="<%= trip.title %>">
  <label for="">Description</label>
  <textarea name="description" rows="4" value="<%= trip.description %>"></textarea>
  <label for="">Image_url</label>
  <input type="text" name="image_url" value="<%= trip.image_url %>">
  <button>Update</button>
</form>